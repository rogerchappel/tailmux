{
  "name": "tailmux",
  "version": "0.1.0",
  "description": "Tailscale-aware tmux workspace launcher and status board.",
  "type": "module",
  "bin": {
    "tailmux": "./dist/src/cli.js"
  },
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "files": [
    "dist/src",
    "dist/tests",
    "scripts/run-tests.mjs",
    "fixtures",
    "examples",
    "README.md",
    "LICENSE",
    "SECURITY.md",
    "CHANGELOG.md",
    "CONTRIBUTING.md"
  ],
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "node scripts/run-tests.mjs",
    "pretest": "npm run build",
    "smoke": "node dist/src/cli.js scan --tailscale fixtures/tailscale-status.json --ssh-config fixtures/ssh_config --ports fixtures/ports.txt --format json >/tmp/tailmux-smoke.json && node dist/src/cli.js template examples/ai-lab.json --dry-run",
    "package:smoke": "node scripts/check-package.mjs",
    "release:check": "npm run check && npm test && npm run smoke && npm run package:smoke",
    "fixtures:scan": "node dist/src/cli.js scan --tailscale fixtures/tailscale-status.json --ssh-config fixtures/ssh_config --ports fixtures/ports.txt",
    "validate": "bash scripts/validate.sh"
  },
  "keywords": [
    "tailscale",
    "tmux",
    "ssh",
    "cli",
    "local-first"
  ],
  "author": "Roger Chappel",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rogerchappel/tailmux.git"
  },
  "bugs": {
    "url": "https://github.com/rogerchappel/tailmux/issues"
  },
  "homepage": "https://github.com/rogerchappel/tailmux#readme"
}
